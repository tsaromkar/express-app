const COLLECTIONS = {
    "Users": "users",
    "Products": "products",
    "ProductTypes": "product-types"
}

const CONSTANTS = {
    secretKey: "my-secret-key"
}

module.exports = { COLLECTIONS, CONSTANTS }